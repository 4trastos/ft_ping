#include "ft_ping.h"

void    ft_statistics(void)
{
    printf("[DEBUG:] -> SEÑAL RECIBIDA: Control + C -> ( %i )\n[DEBUG: Dentro de `ft_statistic.c`] -> Mostrar estadísticas con `statistics.c`\n", SIGINT);
    return;
}